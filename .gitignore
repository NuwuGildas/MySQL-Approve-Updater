/node_modules
/backups
.env
audit.log
connections.json
rules.json
.idea/
/dist
agent.json
agent-chat.json
settings.json

# deploy module runtime data
deploy-repos.json*
deploy-targets.json*
deploy-runs.json*
deploy-secrets.enc
deploy-master.key
/deploy-work
/deploy-runs
deploy-servers.json*
/deploy-keys
deploy-templates.json*
connectors.json
connectors.json.tmp
crash.log

# projects (lib/projects) runtime data
projects.json*

# per-project AI chat history (lib/projects/chat.js) + legacy single chat backup
project-chats.json*
agent-chat.json.*
# managed CLI provider homes (lib/agent-isolation) and one assistant conversation per terminal session
/agent-home/
ssh-session-chats.json
ssh-agent-memory.json

# module host: installed packages, module-owned data and publisher keys
/module-data/
/keys/

# module source lives on its own branches (modules/<id>); scripts/module-worktrees.js
# checks them out here for local work
/.worktrees/

# Module source lives on its own branches (modules/<id>), never in the base
# application's tree. `node scripts/module-worktrees.js --all` checks them out here.
/modules/

# tooling artifacts
/.claude/
/.ui-audit/
